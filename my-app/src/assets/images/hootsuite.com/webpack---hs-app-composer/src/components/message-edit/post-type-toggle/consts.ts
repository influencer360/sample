import translation from 'fe-pnc-lib-hs-translation'

export const IG_REEL_DISABLED_TOOLTIP_TEXT = translation._('Not available with Personal profiles selected')
export const MAX_WIDTH_TOOLTIP = '400px' // the tooltip component adds 28px padding for each side - since the tooltip text is longer, this ensures the padding on either side of the tooltip is even
