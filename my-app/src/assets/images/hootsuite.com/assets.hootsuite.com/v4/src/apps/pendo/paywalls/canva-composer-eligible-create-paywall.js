const paywallPrefix =
    "Paywall|CanvaCampaignUpgrade_Alt1|Free_Users|Composer Activation";
const paywall = "canva_composer";
const TRACKING_ORIGIN = "web.dashboard.composer";

const FreeCreateEligibleCanvaPaywall = {
    paywallPrefix,
    paywall,
    TRACKING_ORIGIN,
};

export default FreeCreateEligibleCanvaPaywall;
