const paywallPrefix = 'Paywall|Scheduled_Post|Free_Users';
const paywall = 'schedule_messages';
const TRACKING_ORIGIN = 'web.dashboard.scheduling_paywall';


const ScheduledMessagesPaywall = {
    paywallPrefix,
    paywall,
    TRACKING_ORIGIN
}

export default ScheduledMessagesPaywall
