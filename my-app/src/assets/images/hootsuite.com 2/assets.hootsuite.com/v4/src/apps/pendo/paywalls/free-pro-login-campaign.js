const paywallPrefix = 'Paywall|Free_To_Pro_Login|Free';
const paywall = 'free_pro_login';
const TRACKING_ORIGIN = 'web.dashboard.free_pro_login';

const FreeProLoginPaywall = {
    paywallPrefix,
    paywall,
    TRACKING_ORIGIN
}

export default FreeProLoginPaywall
